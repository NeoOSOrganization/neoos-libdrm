# `toolchain` comes from the constants file build.sh generates and
# passes as the FIRST --cross-file ($NEOOS_TOOLCHAIN).
[binaries]
c = toolchain / 'bin/x86_64-neoos-linux-musl-gcc'
ar = toolchain / 'bin/x86_64-neoos-linux-musl-gcc-ar'
strip = toolchain / 'bin/x86_64-neoos-linux-musl-strip'
pkg-config = '/usr/bin/pkg-config'

[host_machine]
system = 'linux'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'
